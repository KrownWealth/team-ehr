# ==========================================
# LOCAL DEVELOPMENT ENVIRONMENT
# ==========================================

# Server Configuration
NODE_ENV=development
PORT=8080
API_VERSION=v1

# Database (Local PostgreSQL)
# Install PostgreSQL locally or use Docker:
# docker run --name wecareehr-db -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=wecareehr -p 5432:5432 -d postgres:15
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/wecareehr"

# JWT Configuration
# Generate with: openssl rand -base64 32
JWT_SECRET=local-dev-secret-change-in-production-xxxxxxxxxxxxx
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=local-dev-refresh-secret-change-in-production-xxxxx
JWT_REFRESH_EXPIRES_IN=30d

# OAuth - Google (Optional for local dev)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-local-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-local-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:8080/api/v1/auth/google/callback

# Email Configuration (Gmail)
# For testing, use your personal Gmail
# Get App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com
SMTP_PASSWORD=your-16-char-app-password
FROM_EMAIL=noreply@wecareehr.local

# GCP Configuration (Optional for local - will use mocks)
# Leave empty or comment out for local development
# GCP_PROJECT_ID=wecareehr-dev
# GCP_BUCKET_NAME=wecareehr-dev-storage
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json

# External APIs (Optional)
NIN_API_URL=https://api.nimc.gov.ng
NIN_API_KEY=your-nin-api-key-if-available

# AI Features (Optional)
AI_DIAGNOSIS_FUNCTION_URL=https://your-cloud-function-url
AI_API_KEY=your-ai-api-key

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Rate Limiting (Relaxed for development)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=1000

# Encryption
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Logging Level
LOG_LEVEL=debug